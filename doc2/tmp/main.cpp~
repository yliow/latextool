#include <iostream>
#include <ctime>

int f(int n)
{
    if (n <= 1)
        return 1;
    else
        return f(n - 1) + f(n - 2);
}

int main()
{
    double start, end;
    for (int n = 0; n < 100; n += 10)
    {
        start = clock();
        f(n);
        end = clock();
    }   
}
